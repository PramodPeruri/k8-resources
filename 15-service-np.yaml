apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  type: NodePort
  selector:
    project: roboshop
    component: frontend
    environment: Dev
    purpose: node-port
  ports:
   - protocol: TCP
     port: 80 # Service port
     targetPort: 80 # COntainer port
     