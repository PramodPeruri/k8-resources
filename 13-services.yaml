apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  selector:
    project: roboshop
    component: frontend
    environment: Dev
  ports:
   - protocol: TCP
     port: 80 # Service port
     targetPort: 80 # COntainer port
     